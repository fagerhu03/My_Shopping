import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF2C3E50);  // Dark navy
  static const Color primaryLight = Color(0xFF566778);
  static const Color primaryDark = Color(0xFF1C2A38);

  // Accent Colors
  static const Color accent = Color(0xFFD4AF37);    // Gold
  static const Color accentLight = Color(0xFFFFE082);
  static const Color accentDark = Color(0xFFAF8D24);

  // Background & Surface
  static const Color background = Color(0xFFF5F5F5); // Off-white
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFB00020);

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);  // Near black
  static const Color textSecondary = Color(0xFF616161);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color textOnAccent = Color(0xFF000000);

  // Border Colors
  static const Color borderLight = Color(0xFFE0E0E0);
  static const Color borderDark = Color(0xFFBDBDBD);
}